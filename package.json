{
  "name": "npm-publish-sample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vitest",
    "test": "vitest run",
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "tsc",
    "ci": "pnpm run lint && pnpm run test && pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "tsup": "^7.0.0",
    "vitest": "^0.32.2"
  }
}
